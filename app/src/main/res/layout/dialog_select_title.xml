<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/dialog_title_container"
    android:padding="@dimen/spacing_all_large_small"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <TextView
        android:id="@+id/dialog_title_title"
        android:layout_width="wrap_content"
        android:layout_height="28dp"
        android:text="@string/dialog_title_title"
        android:textColor="@android:color/black"
        android:textSize="20sp"
        android:textStyle="bold"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <RadioGroup
        android:id="@+id/dialog_title_options_list"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:orientation="vertical"
        app:layout_constraintTop_toBottomOf="@+id/dialog_title_title" />

    <TextView
        android:id="@+id/dialog_title_cancel_button"
        android:layout_width="75dp"
        android:layout_height="wrap_content"
        android:layout_marginEnd="8dp"
        android:gravity="center_vertical|center_horizontal"
        android:paddingTop="25dp"
        android:paddingBottom="14dp"
        android:text="@android:string/cancel"
        android:textAllCaps="true"
        android:textColor="@color/accent"
        android:textSize="14sp"
        app:layout_constraintRight_toLeftOf="@+id/dialog_title_ok_button"
        app:layout_constraintTop_toBottomOf="@+id/dialog_title_options_list" />

    <TextView
        android:id="@+id/dialog_title_ok_button"
        android:layout_width="74dp"
        android:layout_height="wrap_content"
        android:gravity="center_vertical|center_horizontal"
        android:paddingTop="25dp"
        android:paddingBottom="14dp"
        android:text="@android:string/ok"
        android:textColor="@color/accent"
        android:textSize="14sp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/dialog_title_options_list" />

</androidx.constraintlayout.widget.ConstraintLayout>